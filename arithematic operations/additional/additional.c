#include <stdio.h>

int main(){

    // Arithematic Operator ++ increment

    int x = 10;
    int y = 3;
    int z = 0;
    int p = 10;
    int q = 10;
    int r = 10;
    int s = 10;

    // augmented assign operators are in / message or green

    // x+=3 addition
    x = x+2;
    // x-=2 subtraction
    y = y-2;
    // x*=2 multiplication
    p = p * 2;
    // x/=2 division
    q = q / 2;

    printf("%d \n", x);
    printf("%d \n", y);
    printf("%d \n", p);
    printf("%d \n", q);
    
    return 0;
}